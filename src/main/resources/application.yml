server:
  port: 8080
  servlet:
    context-path: /welcome


spring:
  thymeleaf:
    cache: false
  profiles:
    active: dev # set deploy profile

logging:
  config: classpath:logback/logback-spring.xml
  file:
    path: ../app-log/demo
    name: demo

welcome:
  message: Stoneberg
